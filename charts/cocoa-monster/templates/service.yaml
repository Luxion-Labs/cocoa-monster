apiVersion: v1
kind: Service
metadata:
  name: {{ include "cocoa-monster.fullname" . }}
  labels:
    {{- include "cocoa-monster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "cocoa-monster.selectorLabels" . | nindent 4 }}
